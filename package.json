{
    "name": "finance-dashboard",
    "version": "1.0.0",
    "description": "Finance Dashboard with Vite React frontend and Node.js backend using nodemon",
    "scripts": {
      "start": "cd server && npm install && npm start",
      "build": "cd client && npm install && npm run build && cd ../server && npm install"
    },
    "engines": {
      "node": "20.x"
    }
  }